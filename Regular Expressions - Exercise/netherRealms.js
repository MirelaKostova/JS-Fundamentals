function netherRealms(txt) {
  let demonsHealh = 0;
  let onlyLetters = txt.replace(/[^A-Za-z]/g, "");

  for (let currLetter of onlyLetters) {
    demonsHealh += Number(currLetter.charCodeAt(0));
  }

  let damage = 0;
  let allNumbers = txt.console.log(sum);
}

netherRealms("M3ph-0.5s-0.5t0.0**");
